import SwiftUI

@main
struct RufusApp: App {
    var body: some Scene {
        Window("rufus4mac", id: "main") {
            ContentView()
        }
        .defaultSize(width: 600, height: 760)
    }
}
